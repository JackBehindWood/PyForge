# PyForge
A modular, standalone build system manager in Python, currently supporting Premake. Designed for easy integration with Git submodules and future extensibility to other build systems like CMake.
